# Practice — daily coding habit

This repository stores small Python practice files. I push daily to build a habit and track activity.

Usage
- Edit your files (e.g., `Practice.py`).
- Run the helper script to commit and push changes:

PowerShell:

```
./push.ps1 -Message "your commit message"
```
